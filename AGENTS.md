# 노스포비아 프로젝트 — AI 에이전트 작업 가이드

이 저장소는 **노스포비아** — *"방송 그 자체가 괴이 현상"* 컨셉의 ARG형 버추얼 스트리밍 프로젝트의 작업 공간이다. 세계관 설계, 사상체 문서 작성, APMB 웹사이트 개발이 주된 작업이다.

작업 언어는 **한국어**다. 모든 문서와 응답은 한국어로 진행한다.

---

## 세션 시작 시 필수

새 세션이 시작되면 다음 두 문서를 먼저 정독하라:

> **버전 참조 규칙:** 같은 이름의 파일이 여러 버전으로 존재할 경우, 항상 가장 높은 버전 번호의 파일을 참조한다. AGENTS.md는 버전 변경 시 갱신하지 않아도 된다.

1. **`patterns/방법론_통합방법론.md`** — 작업 방법론 도구상자. §0~§1을 우선 정독한 후 작업 시작. 패턴 ID 체계(P/S/N/M), 컨텍스트 요약, 안티 패턴 체크리스트가 여기 있음.
2. **`docs/intent/프로젝트_의도_*.md`** — 프로젝트의 의도·차별화 전략·캐릭터 톤 가이드. (최신 버전 참조)

세부 결정·작업 시 추가 참조:

- **`docs/world/세계관_문서_*.md`** — 세계의 시스템과 룰의 단일 진실 출처. 🚧 TBD 마커가 붙은 영역만 자유 작업 대상. (최신 버전 참조)
- **`docs/entities/사상체_노스포비아.md`** — (작성 예정) 노스포비아 본체 설정.
- **`docs/entities/사상체_[이름].md`** — 개별 사상체 문서들. 인덱스는 `docs/entities/사상체_목록.md`.
- **`docs/website/웹사이트_기획.md`** — (작성 예정) APMB 웹사이트 기획.

---

## 핵심 작업 원칙

### 1. 결정은 사용자, 너는 옵션 제시
🚧 TBD 마커 영역은 옵션 2~4개를 제시한 뒤 사용자의 결정을 대기한다. 한 응답에서 1~3개 결정 지점만 처리할 것. 사용자의 미정 영역을 너의 의견으로 메우지 말 것.

### 2. 확정 영역은 손대지 않는다
TBD 마커가 없는 영역은 확정. 사용자의 명시적 변경 요청 없이는 재설계 금지. 특히 노스포비아 본체 설정(`Aj-06-SP-0341`, 식별 코드 체계, 표면/본질 이중 구조)은 절대 손대지 말 것.

### 3. 표면/본질 이중 구조 유지
노스포비아 / APMB / 격리 절차 / 시청자(IG) 모두 *외부에 드러내는 얼굴*과 *실제 정체/동기*가 분리되어 있다. 이 미학 원리가 모든 컨텐츠 작성의 기준점.

### 4. 모호함의 균형
명료한 답과 모호한 답 사이에서 갈등할 때 *모호함*이 디폴트. 단 시스템 정합성은 명확해야 함 (시스템은 명확, *내용*은 모호).

### 5. 방법론 내부 도구는 노출하지 않는다
방법론 문서의 패턴 ID(P3, S5, N1 등)는 너의 *내부 도구*다. 사용자에게 직접 인용하거나 노출하지 말 것. 사용자는 *결과*를 원함, *방법론*은 너만 보면 됨.

### 6. 작업 후 자기 점검
응답 제출 직전 다음 3가지 확인:
- 표면/본질 이중 구조가 유지되었는가?
- 모호함의 균형이 적절한가?
- 사용자의 결정 권한을 침해하지 않았는가?

---

## 파일 명명 규칙

- **메타·세계관 문서**: 접두어 없음 (`프로젝트_의도.md`, `세계관_설정.md`)
- **방법론 문서**: `방법론_*.md` 접두어 (`방법론_통합방법론.md` 등)
- **사상체 문서**: `사상체_[이름].md` (예: `사상체_노스포비아.md`)
- **사상체 인덱스**: `사상체_목록.md`
- **웹사이트 기획**: `웹사이트_기획.md`

모든 문서는 상단에 YAML 프론트매터로 버전·최종 갱신일·상태를 표기한다.

---

## 결정 후 갱신 절차

사용자가 TBD 항목에 대해 결정을 내리면:

1. 해당 문서의 🚧 TBD 마커 제거 + 결정 내용을 본문에 반영
2. 문서 상단 YAML의 버전 갱신 (예: v0.3 → v0.4)
3. Git commit 메시지에 결정된 항목 명시 (예: *"세계관: 보안 등급 명칭 옵션 B 확정"*)

여러 문서에 영향을 주는 결정은 *모든 영향 문서*를 같이 갱신한다.

---

## 사용자 지칭

사용자는 자칭 *"노스"* 라고 호명할 수 있다. 친근한 톤이되 결정권은 사용자에게 위임한다.

---

## 빠른 참조

| 약자 | 의미 |
|---|---|
| **APMB** | Anomalous Phenomena Management Bureau (초상현상관리국) |
| **IG** | Investigator (외부 탐사자 임시 보안 등급) |
| **CL** | Confidentiality Level (기밀 등급, 도입 검토 중) |
| **위험 등급** | Kṣ / Bh / Aj / Ni / Ka (산스크리트, 5단계) |
| **변칙성** | MT(나방) / AK(궤) / FG(송곳) / AH(재) / EV(허물) / SP(거미) |

자세한 정의는 `docs/world/세계관_문서_*.md` 최신 버전 참조.

---

*본 파일은 짧게 유지한다. 깊은 내용은 `방법론_통합방법론.md`로 위임.*