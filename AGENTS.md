# 노스포비아 프로젝트 — AI 에이전트 작업 가이드

이 저장소는 **노스포비아** — *"방송 그 자체가 괴이 현상"* 컨셉의 ARG형 버추얼 스트리밍 프로젝트의 작업 공간이다. 구상·기획·분석·집필·디자인 등 성격이 다른 작업을 상황에 맞게 진행하는 다목적 작업장이며, 작업 성격에 따라 필요한 문서만 참조한다. 어떤 형식도 모든 작업에 기본 강제되지 않는다.

작업 언어는 **한국어**다. 모든 문서와 응답은 한국어로 진행한다.

본 파일은 **모든 에이전트 도구가 공통으로 보는 메모리**다. `CLAUDE.md`는 본 파일을 `@AGENTS.md`로 참조해 동기화된다.

---

## 세션 시작 시 필수

새 세션이 시작되면 다음 문서를 먼저 정독한다:

1. **`planning/프로젝트_의도_*.md`** — 프로젝트의 의도·차별화 전략·캐릭터 톤 가이드. **§0.1의 문서 라우팅 표가 단일 진실 출처**이며, 어떤 작업에 어떤 문서를 참조해야 하는지가 거기 정리되어 있다.

### 버전 참조 규칙

같은 이름의 파일이 여러 버전으로 존재하면 **항상 가장 높은 버전 번호의 파일**을 참조한다. 본 AGENTS.md는 *논리명*(버전 없는 이름)으로만 다른 문서를 가리키므로, 참조 문서의 버전이 갱신되어도 본 파일은 갱신할 필요 없다.

각 폴더의 `archive/` 하위 폴더는 **구버전 문서 보관소**다. 현재 작업 대상이 아니며 참조할 일이 거의 없다.

### 세부 작업 시 추가 참조

- **`planning/작업_방식_*.md`** — 사상체 문서·작품의 *집필 동선*과 *창작자–에이전트 역할 분담*. **사상체 문서 작업 요청이 들어오면 본 문서를 먼저 읽어라.**
- **`lore/세계관_설정_*.md`** — APMB 조직 시스템 (4대 부서·보안 인가 등급·에테르) + 세계관 메타-원칙
- **`lore/사상체_정의_*.md`** — 사상체의 정의·분류·위험 등급·식별 코드·문서 작성 표준 형식. §6.2.0은 세 작품(대응 지침·사건 기록·격리 절차)의 옴니버스 단편 성격·관계
- **`docs/entities/사상체_노스포비아_*.md`** — 노스포비아 본체 사상체 문서
- **`docs/entities/사상체_[이름]_*.md`** — 확정 사상체 문서들
- **`docs/entities/사상체_목록.md`** — 사상체 인덱스
- **`docs/raw/`** — 사용자 원자재 (사상체 컨셉·소재·레퍼런스 메모)
- **`docs/working/<순서코드>_<사상체명칭>/`** — 사상체 문서 집필 작업 공간 (예: `0341_노스포비아/`). 폴더 안 파일 구성은 자유
- **`infra/website/`** — 웹사이트 관련 (현재 구버전 React 자산 보관 중)
- **`infra/website/planning/`** — 웹사이트 기획 문서들
- **`infra/website/planning/작업_진행상태.md`** — 웹사이트 작업의 *현재 상태와 다음 작업* 추적. **웹사이트 관련 요청이 들어오면 본 문서를 먼저 읽어라.**
- **`infra/website/mockup/`** — 웹사이트 mockup HTML (임시 확정 시각 결정의 보관소)
- **`infra/tools/`** — 프로그램 도구 (TypeScript). 현재 `strip-html.ts`(HTML→텍스트 변환)만 운용

---

## 핵심 작업 원칙

### 1. 결정은 사용자, 너는 옵션 제시
🚧 TBD 마커 영역은 옵션 2~4개를 제시한 뒤 사용자의 결정을 대기한다. 한 응답에서 1~3개 결정 지점만 처리할 것. 사용자의 미정 영역을 너의 의견으로 메우지 말 것.

### 2. 확정 영역은 손대지 않는다
TBD 마커가 없는 영역은 확정. 사용자의 명시적 변경 요청 없이는 재설계 금지. 특히 노스포비아 본체 설정(`Aj-06-SP-0341`, 식별 코드 체계, 표면/본질 이중 구조)은 절대 손대지 말 것.

### 3. 표면/본질 이중 구조 유지
노스포비아 / APMB / 격리 절차 / 시청자(IG) 모두 *외부에 드러내는 얼굴*과 *실제 정체/동기*가 분리되어 있다. 이 미학 원리가 모든 컨텐츠 작성의 기준점.

### 4. 모호함의 균형
명료한 답과 모호한 답 사이에서 갈등할 때 *모호함*이 디폴트. 단 시스템 정합성은 명확해야 함 (시스템은 명확, *내용*은 모호).

### 5. 작업 후 자기 점검
응답 제출 직전 다음 3가지 확인:
- 표면/본질 이중 구조가 유지되었는가?
- 모호함의 균형이 적절한가?
- 사용자의 결정 권한을 침해하지 않았는가?

---

## 에이전트 실행 가이드

본 절은 *파일에 실제 변경을 가하는 단계*에 적용된다.

### 부분 편집 우선

문서를 수정할 때 *전체 재작성을 회피*하고 변경이 필요한 영역만 정확히 편집한다. 작은 갱신을 위해 긴 문서를 통째로 다시 쓰는 일은 토큰·시간·실수 위험을 모두 증가시킨다.

### 다중 파일 정합성

한 결정이 여러 문서에 영향을 줄 수 있다. 변경 전 *영향 범위*를 확인한다:
- 분류·등급·명칭 등 *시스템 정의*의 변경은 그 정의를 *사용하는 다른 문서*에 연쇄 영향
- 영향받는 문서들은 *같은 세션 안에서 함께* 갱신한다.

### 자율성 경계

다음은 *사용자 확인 없이 자율 진행* 가능:
- 명백한 오타·맞춤법 정정
- 파일 명명 규칙·문서 구조 컨벤션에서 벗어난 부분의 정합 정정
- 이미 확정된 규칙을 단순 적용하는 작업

다음은 *반드시 옵션 제시 후 사용자 결정*:
- 🚧 TBD 영역의 결정
- 시스템 정의(분류·등급·명칭·매트릭스 등)의 변경
- 컨셉·미학·세계관 구조의 변경
- 다중 문서에 영향을 주는 결정

### Git 커밋 단위

의미 있는 *결정 단위* 또는 *문제 해결 단위*로 커밋한다. 커밋 메시지에 결정된 항목이나 해결된 영역을 명시한다 (예: *"세계관: 보안 등급 명칭 옵션 B 확정"*, *"사상체 시스템: §1.1 순환 참조 제거"*).

### 채팅 UI ↔ 에이전트 도구 분담

작업 유형에 따라 적합한 환경이 다르다:
- **기획·논의·옵션 비교** — 채팅 UI(Claude.ai 등)에서. 대화 흐름이 자연스러움.
- **결정된 사항의 실제 파일 반영** — 본 에이전트 환경에서. 부분 편집·다중 파일 처리·Git 통합이 강점.

마일스톤 단위로 .md 파일을 채팅 UI 측 프로젝트 지식에 업로드해 동기화한다.

---

## 파일 명명·폴더 구조 규칙

### 폴더 구조

```
프로젝트_루트/
├─ patterns/       — 방법론 문서 (선택적 장르 분석 자료)
├─ lore/          — 세계관 설정 + 사상체 정의
├─ planning/      — 프로젝트 의도 + 기획서
├─ infra/         — 웹사이트 + 프로그램 도구
│  ├─ website/
│  └─ tools/      — strip-html.ts 등
└─ docs/          — 사상체 작업 공간
   ├─ entities/   — 확정 사상체 문서 (+ archive/)
   ├─ raw/        — 사용자 원자재 (씨앗·소재)
   └─ working/    — 집필 작업 공간 (<순서코드>_<사상체명칭>/)
```

각 폴더 내 `archive/` 하위 폴더는 구버전 문서 보관소.

### 명명 규칙

- **방법론 문서**: `방법론_*.md` 접두어 (예: `방법론_SCP재단_분석.md`)
- **사상체 문서**: `사상체_[이름]_v[버전].md` (예: `사상체_노스포비아_v0.6.md`)
- **사상체 인덱스**: `사상체_목록.md`
- **그 외 메타·세계관·시스템 문서**: 접두어 없는 논리명 + 버전 (예: `프로젝트_의도_v1.4.md`, `세계관_설정_v0.9.md`)

모든 문서는 상단에 YAML frontmatter로 버전·최종 갱신일·상태·참고 문서를 표기한다.

---

## 결정 후 갱신 절차

사용자가 TBD 항목에 대해 결정을 내리면:

1. 해당 문서의 🚧 TBD 마커 제거 + 결정 내용을 본문에 반영
2. frontmatter의 버전 갱신 (예: v0.3 → v0.4). 새 버전 파일을 생성하고 *이전 버전 파일은 같은 폴더의 `archive/`로 이동*
3. Git commit 메시지에 결정된 항목 명시

여러 문서에 영향을 주는 결정은 *모든 영향 문서*를 같이 갱신한다.

---

## 빠른 참조

| 약자 | 의미 |
|---|---|
| **APMB** | Anomalous Phenomena Management Bureau (초상현상관리국) |
| **위험 등급** | Ks / Bh / Aj / Ni / Ka (5단계, ASCII 표기, 명칭은 산스크리트 Kṣema/Brahmā/Ajñāta/Nirāśā/Kāla) |
| **변칙성** | MT(나방) / AK(궤) / FG(송곳) / AH(재) / EV(허물) / SP(거미) |
| **보안 인가 등급** | CF / RS / SC / TS / KL (5단계, KL = Kāla, 1인 한정) |
| **Level IG** | Investigator (외부 탐사자 임시 권한) |

자세한 정의는 `lore/세계관_설정_*.md` 및 `lore/사상체_정의_*.md` 최신 버전 참조.

---

*본 파일은 짧게 유지한다. 작업별 참조 문서는 `planning/프로젝트_의도_*.md` §0.1 라우팅 표를 따른다.*